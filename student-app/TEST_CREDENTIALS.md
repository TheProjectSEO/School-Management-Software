# Test Login Credentials

Your Supabase authentication has been successfully configured! All users have confirmed emails and can log in immediately.

## 🎓 Student Portal Test Accounts

### Student Account
- **Email:** `student@msu.edu.ph`
- **Password:** `Test123!@#`
- **Name:** Juan Dela Cruz
- **Role:** Student
- **User ID:** `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa`

### Teacher Account
- **Email:** `teacher@msu.edu.ph`
- **Password:** `Test123!@#`
- **Name:** Maria Santos
- **Role:** Teacher
- **User ID:** `862a76b7-789f-4056-be9f-879ff006a5ca`

### Admin Account
- **Email:** `admin@msu.edu.ph`
- **Password:** `Admin123!@#`
- **Name:** Admin User
- **Role:** Admin
- **User ID:** `2da60adc-ea62-4016-90b8-984795fa7305`

---

## 🔐 Authentication Details

**Authentication Method:** Supabase Auth (JWT-based)
- ✅ Email confirmation: Disabled (all emails pre-confirmed)
- ✅ JWT tokens: Automatically generated by Supabase
- ✅ Session management: Handled by Supabase Auth
- ✅ Password encryption: bcrypt with salt

**Database Tables:**
- `auth.users` - Authentication records (managed by Supabase)
- `public.user_profiles` - Extended user data (managed by your app)

---

## 🚀 How to Test

1. **Start your development server:**
   ```bash
   npm run dev
   ```

2. **Navigate to login page:**
   ```
   http://localhost:3000/login
   ```

3. **Try logging in with any of the test accounts above**

4. **After login, you should be redirected to:**
   ```
   http://localhost:3000/
   ```

---

## 🔍 What Was Fixed

**Problem:** You had a user profile in `public.user_profiles` but no corresponding authentication record in `auth.users`

**Solution:** Created matching authentication records using Supabase MCP with:
- Properly hashed passwords (bcrypt)
- Confirmed email addresses
- User metadata for roles and names
- JWT-compatible authentication setup

---

## 📝 Important Notes

1. **Supabase Auth IS JWT Authentication** - You don't need custom JWT. Supabase already:
   - Generates JWTs automatically
   - Manages token refresh
   - Handles session persistence
   - Provides OAuth integration

2. **Password Security** - Passwords are hashed using bcrypt with salt
   - Never stored in plain text
   - Industry-standard encryption
   - Supabase handles this automatically

3. **Production Recommendations:**
   - Change these test passwords before going live
   - Enable email verification for production
   - Set up Row Level Security (RLS) policies
   - Configure proper OAuth providers if needed

---

## 🛠️ Troubleshooting

### "Invalid login credentials" error?
- Verify you're using the exact email/password from this document
- Check caps lock is off
- Try clearing browser cookies

### Still can't log in?
Run this SQL query in Supabase to verify the user exists:
```sql
SELECT email, email_confirmed_at, raw_user_meta_data
FROM auth.users
WHERE email = 'student@msu.edu.ph';
```

### Want to add more test users?
Use the Supabase MCP or run SQL directly:
```sql
INSERT INTO auth.users (
    id, instance_id, email, encrypted_password,
    email_confirmed_at, aud, role, raw_app_meta_data,
    raw_user_meta_data, created_at, updated_at
)
VALUES (
    gen_random_uuid(),
    '00000000-0000-0000-0000-000000000000',
    'newuser@msu.edu.ph',
    crypt('YourPassword123', gen_salt('bf')),
    now(), 'authenticated', 'authenticated',
    '{"provider":"email","providers":["email"]}'::jsonb,
    '{"full_name":"New User","role":"student"}'::jsonb,
    now(), now()
);
```

---

**Created:** December 31, 2025 via Supabase MCP
**Status:** ✅ Ready to test
